# Django Configuration
# Generate a new secret key with: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=your-secret-key-here
DEBUG=True

# Database Configuration
DB_NAME=tunzadent
DB_USER=your_database_user
DB_PASSWORD=your_database_password
DB_HOST=localhost
DB_PORT=3306

# Email Configuration (Gmail SMTP)
# Use an app-specific password: https://support.google.com/accounts/answer/185833
EMAIL_HOST_USER=your_email@gmail.com
EMAIL_HOST_PASSWORD=your_app_password_here

# Frontend URL (for email links)
FRONTEND_URL=http://localhost:3000